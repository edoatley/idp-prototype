{
  "name": "idp-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Thin custom IDP portal — create-bucket write path (Phase 3).",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^3.2.6"
  }
}
