{
  "name": "idp-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "IDP prototype — npm workspaces for the platform's Node surfaces (core domain, portal, CLI).",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "idp-core",
    "idp-portal"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint:api": "redocly lint contracts/openapi.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "^2.51.2"
  }
}
